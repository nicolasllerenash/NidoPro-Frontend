# URL del backend NestJS
# En producción (EC2): apunta a la IP pública del servidor.
# Nginx hace proxy de /api/ -> http://127.0.0.1:3000, así que el frontend
# puede usar la misma IP del servidor sin especificar puerto.
VITE_API_URL=http://52.14.139.176/api/v1

# Entorno de la aplicación
VITE_NODE_ENV=production
